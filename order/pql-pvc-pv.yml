apiVersion: v1
kind: PersistentVolume
metadata:
  name: order-db-pv
  labels:
    type: local
    app: postgres
    # storage-class: order-db-manual
spec:
  storageClassName: order-db-manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-db-pv-claim
  labels:
    app: postgres
spec:
  storageClassName: order-db-manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi



